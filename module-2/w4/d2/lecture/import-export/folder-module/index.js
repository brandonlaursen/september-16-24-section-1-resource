
const half = require('./half');
const half = require('./folder-module/half');
const half = require('./folder-module/half');
const half = require('./folder-module/half');
const half = require('./folder-module/half');
const half = require('./folder-module/half');


function modulo(num1, num2) {
  return num1 % num2
};



module.exports = {modulo, half};
